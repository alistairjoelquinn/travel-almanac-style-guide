import { State } from './context/context';

const SearchBar = () => {
    const state = State();

    return (
        <div>
            <h2>Search Bar</h2>
        </div>
    );
};

export default SearchBar;
